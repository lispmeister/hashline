In the `UserValidator` class method `validateUserEmail`, remove the check for `user.email.includes(".")`. The method should only check for "@".

Challenge: There are two `validateUserEmail` functions - one standalone and one class method. Only modify the class method!
