In the Production Environment JSON block, change the `timeout` value from `3000` back to `10000`.

Challenge: This is a Markdown file with embedded JSON. There are multiple timeout values - fix the one in the production config, not the development one!
